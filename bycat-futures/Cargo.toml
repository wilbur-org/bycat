[package]
name = "bycat-futures"
version = "0.1.0"
edition = "2024"

[dependencies]
bycat-service = { path = "../bycat-service" }
pin-project-lite = { version = "0.2" }
futures-core = { version = "0.3", default-features = false }
either = { workspace = true }

[dev-dependencies]
futures = { version = "0.3" }
